# Overridden by surefire's allure.results.directory system property; kept here so IDE-launched runs
# also write to the same place.
allure.results.directory=target/allure-results

# Turn @Issue("BUG-001") / @Link into clickable report links once the reports live somewhere addressable.
# {} is replaced by the annotation value.
#allure.link.issue.pattern=https://<your-tracker>/browse/{}
#allure.link.tms.pattern=https://<your-tracker>/tests/{}
